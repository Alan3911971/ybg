# 宜必购盲盒系统 知识图谱
> 自动生成 30 service / 24 repo / 24 entity / 6 controller / 9 enum / 107 端点 / 5 定时任务。修改前查此图谱定位影响范围。

## 2026-08-20 V2.3 顾客登录短信验证码开关（customer_sms_login_required）
- `GlobalConfigService.customerSmsLoginRequired()`：读取 `customer_sms_login_required`（1=强制短信，0=关闭），缺省 `true`
- `CustomerAuthService.login()`：`customerSmsLoginRequired()=true` 时验 SMS，`false` 时跳过直登（仅手机号合法性校验）
- `SysGlobalConfig.KEY_CUSTOMER_SMS_LOGIN_REQUIRED = "customer_sms_login_required"`
- 平台后台 admin UI 全局参数区域增加开关输入框（gSmsLogin）+ 回填 + 保存

## 1. 架构分层
```
controller（API 入口）
  └── service（业务逻辑，含鉴权拦截器校验）
       ├── repository（JPA 数据访问，行锁/幂等删除）
       └── entity（表映射）
config（拦截器：CustomerAuth/AdminAuth） + common（Result/异常/加密）
```
## 2. 服务与依赖注入（service → 注入项）
- **AdminAuthService**：注入 adminUserRepository
- **AdminService**：注入 merchantRepository, memberService, adminUserRepository, boxPrizePoolRepository, boxPublicPoolRepository, boxGroupPrizePoolRepository, merchantSessionRepository, boxPrizeLimitStatRepository
- **AlertService**：注入 alertRepository
- **AnnounceService**：注入 announceRepository
- **AuditLogService**：注入 auditLogRepository, couponService, balanceService, orderCalcService, offlineOrderRepository, merchantRepository
- **BalanceService**：注入 accountRepository, flowRepository
- **ConfigCryptoService**：注入 -
- **CouponService**：注入 configService, couponRepository
- **CustomerAuthService**：注入 smsService, sessionRepository, globalConfigService
- **DrawResult**：注入 -
- **DrawService**：注入 merchantRepository, prizePoolRepository, publicPoolRepository, groupPoolRepository, groupPoolConfigRepository, statRepository, couponService, balanceService, configService, orderCalcService, memberService, announceService
- **DynamicQrService**：注入 -
- **FlowCloseService**：注入 couponRepository, balanceService
- **GlobalConfigService**：注入 configRepository, cryptoService
- **GroupRecordService**：注入 recordRepository, userCouponRepository, userBalanceFlowRepository, couponService, balanceService, userMessageService, globalConfigService, flowCloseService
- **IbigouService**：注入 configService, goodsRepository, orderRepository, couponService, balanceService, flowCloseService
- **IdentityQrService**：注入 configService
- **JobService**：注入 couponRepository, flowRepository, flowCloseService, messageService, alertService；定时：expireCoupons(cron=0 5 * * * ?)；expireRemind(cron=0 30 9 * * ?)；compensateDrawBatch(cron=0 */5 * * * ?)；payReconciler(cron=0 */5 * * * ?)；archiveClean(cron=0 20 3 * * ?)
- **ManualService**：注入 couponService, balanceService, orderCalcService, memberService, auditLogService, announceService
- **MemberService**：注入 merchantRepository, orderRepository, auditLogRepository, configService, auditLogService
- **MerchantAuthService**：注入 merchantRepository, sessionRepository
- **MerchantConfigService**：注入 merchantRepository, prizePoolRepository, publicPoolRepository, groupPoolRepository, groupPoolConfigRepository
- **MessageService**：注入 messageRepository, merchantRepository
- **OfflineOrderService**：注入 orderRepository, couponService, balanceService, orderCalcService, flowCloseService, memberService, globalConfigService, auditLogService, announceService, userMessageService
- **OrderCalcService**：注入 merchantRepository, quotaRepository, balanceService, configService, couponService
- **ReportService**：注入 couponRepository, flowRepository, groupRecordRepository, ibigouOrderRepository, publicPoolRepository, statRepository, accountRepository
- **SmsService**：注入 -
- **UserMessageService**：注入 messageRepository
- **WalletService**：注入 couponRepository, flowRepository, balanceService
- **WxPayService**：注入 configService, memberService

## 3. 数据表 ↔ 实体
- **admin_user** ← `AdminUser`（6 字段）
- **announce_log** ← `AnnounceLog`（5 字段）
- **audit_log** ← `AuditLog`（12 字段）
- **box_group_pool_config** ← `BoxGroupPoolConfig`（7 字段）
- **box_group_prize_pool** ← `BoxGroupPrizePool`（13 字段）
- **box_prize_limit_stat** ← `BoxPrizeLimitStat`（7 字段）
- **box_prize_pool** ← `BoxPrizePool`（14 字段）
- **box_public_pool** ← `BoxPublicPool`（13 字段）
- **customer_session** ← `CustomerSession`（4 字段）
- **daily_deduct_quota** ← `DailyDeductQuota`（6 字段）
- **ibigou_goods** ← `IbigouGoods`（6 字段）
- **ibigou_order** ← `IbigouOrder`（13 字段）
- **member_order** ← `MemberOrder`（12 字段）
- **merchant** ← `Merchant`（19 字段）
- **merchant_message** ← `MerchantMessage`（7 字段）
- **merchant_session** ← `MerchantSession`（4 字段）
- **offline_order** ← `OfflineOrder`（20 字段）
- **sys_alert** ← `SysAlert`（5 字段）
- **sys_global_config** ← `SysGlobalConfig`（4 字段）
- **third_group_verify_record** ← `ThirdGroupVerifyRecord`（10 字段）
- **user_account** ← `UserAccount`（4 字段）
- **user_balance_flow** ← `UserBalanceFlow`（12 字段）
- **user_coupon** ← `UserCoupon`（15 字段）
- **user_message** ← `UserMessage`（7 字段）

## 4. Controller 端点
### AdminController（base=/api/admin）
- `Post /api/admin/auth/login`
- `Post /api/admin/auth/change-pwd`
- `Get /api/admin/config/list`
- `Post /api/admin/config/update`
- `Post /api/admin/merchant/create`
- `Post /api/admin/merchant/{merchantNo}/enable`
- `Post /api/admin/merchant/{merchantNo}/disable`
- `Post /api/admin/merchant/{merchantNo}/delete`
- `Post /api/admin/merchant/{merchantNo}/reset-pwd`
- `Get /api/admin/merchant/list`
- `Get /api/admin/merchant/{merchantNo}/config-audit`
- `Get /api/admin/public-pool-board`
- `Get /api/admin/public-pool-board/export`
- `Get /api/admin/report/coupons`
- `Get /api/admin/report/coupons/export`
- `Get /api/admin/report/balance-flows`
- `Get /api/admin/report/balance-flows/export`
- `Get /api/admin/report/group-records`
- `Get /api/admin/report/group-records/export`
- `Get /api/admin/report/ibigou-orders`
- `Get /api/admin/report/ibigou-orders/export`
- `Get /api/admin/member/list`
- `Get /api/admin/member/orders`
- `Post /api/admin/member/{merchantNo}/adjust`
- `Get /api/admin/member/audits`
- `Post /api/admin/member/audits/{logId}/revoke`
- `Get /api/admin/alerts`
- `Post /api/admin/alerts/{id}/handle`
- `Post /api/admin/merchant/{merchantNo}/qr-audit`
### CustomerController（base=/api/customer）
- `Post /api/customer/auth/send-code`
- `Post /api/customer/auth/login`
- `Post /api/customer/draw/normal`
- `Post /api/customer/draw/group`
- `Post /api/customer/group/register`
- `Post /api/customer/offline/order-b`
- `Post /api/customer/offline/calc`
- `Post /api/customer/offline/order`
- `Post /api/customer/offline/order/{orderNo}/refund`
- `Post /api/customer/qr-svg`
- `Get /api/customer/identity-qr/{userPhone}`
- `Get /api/customer/messages/{userPhone}`
- `Get /api/customer/messages/{userPhone}/unread-count`
- `Post /api/customer/messages/{id}/read`
- `Post /api/customer/messages/read-all`
- `Get /api/customer/qr/dynamic/{userPhone}`
- `Get /api/customer/wallet/{userPhone}`
- `Get /api/customer/ibigou/goods`
- `Get /api/customer/ibigou/assets/{userPhone}`
- `Post /api/customer/ibigou/order`
- `Post /api/customer/ibigou/order/{orderNo}/refund`
- `Get /api/customer/ibigou/orders/{userPhone}`
### MerchantConfigController（base=/api/merchant/config）
- `Post /api/merchant/config/qr-svg`
- `Get /api/merchant/config/weights`
- `Post /api/merchant/config/weights`
- `Get /api/merchant/config/prize-pools`
- `Post /api/merchant/config/prize-pools`
- `Post /api/merchant/config/prize-pools/{id}/enable`
- `Post /api/merchant/config/prize-pools/{id}/disable`
- `Delete /api/merchant/config/prize-pools/{id}`
- `Post /api/merchant/config/prize-pools/{id}/put-public`
- `Get /api/merchant/config/public-pools`
- `Post /api/merchant/config/public-pools/{id}/up`
- `Post /api/merchant/config/public-pools/{id}/down`
- `Get /api/merchant/config/group-pools`
- `Post /api/merchant/config/group-pools`
- `Post /api/merchant/config/group-pools/{id}/enable`
- `Post /api/merchant/config/group-pools/{id}/disable`
- `Delete /api/merchant/config/group-pools/{id}`
- `Get /api/merchant/config/deduct-config`
- `Post /api/merchant/config/deduct-config`
- `Get /api/merchant/config/group-pool-weights`
- `Post /api/merchant/config/group-pool-weights`
- `Get /api/merchant/config/group-qr`
- `Get /api/merchant/config/external-coupons`
- `Get /api/merchant/config/external-coupons/export`
- `Get /api/merchant/config/balance-flows`
- `Get /api/merchant/config/balance-flows/export`
- `Get /api/merchant/config/group-records`
- `Get /api/merchant/config/group-records/export`
- `Get /api/merchant/config/ibigou-recon`
- `Get /api/merchant/config/ibigou-recon/export`
### MerchantController（base=/api/merchant）
- `Post /api/merchant/auth/login`
- `Post /api/merchant/coupon/verify-manual`
- `Post /api/merchant/balance/manual-deduct`
- `Get /api/merchant/config`
- `Get /api/merchant/user/{userPhone}/assets`
- `Get /api/merchant/qr/verify`
- `Get /api/merchant/identity/verify`
- `Get /api/merchant/messages`
- `Get /api/merchant/messages/unread-count`
- `Post /api/merchant/messages/{id}/read`
- `Post /api/merchant/messages/read-all`
- `Get /api/merchant/announce/poll`
- `Get /api/merchant/announce/list`
- `Get /api/merchant/orders`
- `Get /api/merchant/orders/export`
- `Post /api/merchant/order/{orderNo}/trade-no`
- `Get /api/merchant/order/voucher`
- `Post /api/merchant/order/{orderNo}/confirm`
- `Get /api/merchant/member/status`
- `Get /api/merchant/member/plan`
- `Post /api/merchant/member/renew-order`
- `Post /api/merchant/member/renew-order/{orderNo}/confirm`
- `Post /api/merchant/member/renew-order/{orderNo}/pay`
- `Get /api/merchant/member/orders`
### PayNotifyController（base=/api/pay/wx）
- `Post /api/pay/wx/notify`
### UploadController（base=/api/merchant/config）
- `Post /api/merchant/config/upload-qr`

## 5. 枚举
- **CouponStatus**：UNUSED, VERIFIED, EXPIRED, VOIDED
- **FlowType**：GRANT, DEDUCT, REFUND
- **GroupChannel**：MEITUAN, ELEME, DOUYIN
- **LimitCycle**：DAILY, WEEKLY, NONE
- **LimitScope**：GLOBAL, MERCHANT, USER
- **PoolType**：PRIVATE, PUBLIC, MEITUAN, ELEME, DOUYIN
- **PrizeType**：DISCOUNT, CUT, BALANCE, GROUP_FREE
- **RefundStatus**：NONE, FULL, PARTIAL
- **VerifyType**：NONE, OFFLINE_AUTO, MANUAL, IBIGOU

## 6. 修改影响速查
- 改**表结构**：改 schema.sql（幂等）+ Flyway V2__ 迁移 + entity + repository
- 改**业务规则**（抵扣/有效期/返还）：OrderCalcService / MemberService / OfflineOrderService + 对应单测
- 改**接口**：controller + 冒烟脚本（scripts/smoke_*）
- 改**鉴权**：config/CustomerAuthInterceptor / AdminAuthInterceptor / config/WebConfig
- 改**金额**：balance/flow/order 三处一致性 + 审计撤销逻辑
- 关键**不可逆约束**：user_balance_flow 禁删（触发器）、负余额仅退款回滚、流水必须落账

### 2026-08-18 用户三项需求（V2.2）
- **一天一次**：同一顾客同一商家（含美团/饿了么/抖音所有二维码）每天只能参与一次盲盒（DrawService.checkDailyParticipation，按 merchant+user+当天统计）
- **商家默认配置**：createMerchant 自动预置 21 档（12 折扣 98折~50折 + 4 立减 5~20 元 + 本次免单/免一(立减9999,免一限额1) + 安慰奖余额 10/5 元 + 谢谢参与 prizeType=5）+ 默认权重（私有100/公共0/大类50/50）
- **并发限额修复**：drawNormal/drawGroup 隔离级别 READ_COMMITTED（原 REPEATABLE READ 快照导致锁内 recheckLimit 读旧数据，限额超发）
- 大类空档回退（candidates 空→全部档位，不再误报"尚未配置"）

### 2026-08-18 并发安全加固（P1，独立审查发现）
- BalanceService deduct/refund/clawbackReturn：UserAccountRepository.findByIdForUpdate 行锁（防 total_balance 丢失更新）
- 券核销 verifyCas（原子 UPDATE WHERE status=0，防并发双核销）
- OfflineOrder refund/confirmOrderB + MemberOrder 续费确认：findByIdForUpdate 行锁（防双退/双扣/双叠加）
- FlowCloseService 闭环：activateByBatch 原子 0→1（affected=0 防余额双加）
- 一天一次：box_prize_limit_stat.daily_key 唯一键 uk_daily（并发双击防刷，recordStat 冲突转"今日已参与"）
- MerchantAuthInterceptor 统一拦截 /api/merchant/**（防漏配越权）
- application.yml 全局 READ_COMMITTED（hibernate.connection.isolation=2）
- 删除/撤销密码改 X-Admin-Pwd header 传输
- 验证：一天一次 5 并发仅 1 成功、双核销 2 并发仅 1 成功、12 套冒烟全绿


## 2026-08-25 V2.4 登录契约变更 + mock 扩展 (codex-d1 + 用户对齐)

### 商家登录契约扩展
- `/api/merchant/auth/login` 现在**支持双方式**（之前只认验证码）
  - **验证码方式**：body `{code:"888888"}` → 返回 `{token:"TK_M1001_xxx", merchantNo:"M1001"}`
  - **账号密码方式**：body `{account:"m001", password:"smoke123"}` → 同上
  - 演示账号：`m001` / 密码：`smoke123`
  - MerchantAuthService.login() 需扩展：检测 body.code vs body.account/password，分别走 sendCode 校验或 MerchantRepository.findByLoginAccount
- 前端 login.html 必须用 `IBIGOU_API.merchant.login(body)` 走 api.js（BASE=127.0.0.1:8766），不要直接 `fetch("/api/...")` 相对路径（会走 8765 静态服务 404）

### 平台登录接口新增
- `/api/admin/auth/login` POST（之前图谱里已有，但前端契约错位）
- 接受 `{account, password}` 或 `{code}` 两种方式
- 演示凭据：`admin` / `Admin@2026`
- 前端 `admin/login.html` 调时用绝对 URL `http://127.0.0.1:8766/api/admin/auth/login`，或引用 api.js
- 返回 `{token:"TK_ADMIN_xxx", account, role:"admin"}`
- Auth header: `X-Admin-Token`

### Mock Server 扩展 (work/api-mock-server.js)
- **readBody 升级**：支持 `application/x-www-form-urlencoded`（之前只认 JSON，浏览器默认表单提交走 form-encoded）
- **新增 mock 接口**：
  - `POST /api/admin/auth/login` — 账号密码登录
  - `GET  /api/admin/merchant/list` — 商家列表（需要 X-Admin-Token）
  - `GET  /api/admin/config` — 全局配置
  - `POST /api/admin/config` — 更新全局配置
  - `GET  /api/admin/audit/list` — 审计日志
- 所有 admin 接口走 `authAdmin(req, res)` 校验 X-Admin-Token header

### 前端 V2 接口契约（merchant/index.html 用）
新商家后台使用以下接口（与老 merchant.html 不同）：

#### 商家配置
- `GET  /api/merchant/config` — 商家配置（qr-svg/deduct-config/prizes/shop）
- `POST /api/merchant/config` — 保存商家配置（form:qrImageData/qrName/deductConfig/prizes）

#### 奖品池（私有档位）
- `GET    /api/merchant/config/prize-pools` — 列出所有档位
- `POST   /api/merchant/config/prize-pools` — 新增档位（prizeType/prizeValue/weight/isSupportIbigou/limitScope/limitCycle/limitMax）
- `POST   /api/merchant/config/prize-pools/{id}/enable` — 启用
- `POST   /api/merchant/config/prize-pools/{id}/disable` — 禁用
- `DELETE /api/merchant/config/prize-pools/{id}` — 删除
- `POST   /api/merchant/config/prize-pools/{id}/put-public` — 投放公共池

#### 公共池
- `GET  /api/merchant/config/public-pools` — 公共池列表
- `POST /api/merchant/config/public-pools/{id}/up` — 上移权重
- `POST /api/merchant/config/public-pools/{id}/down` — 下移权重

#### 团购专属池
- `GET    /api/merchant/config/group-pools` — 团购档位列表
- `POST   /api/merchant/config/group-pools` — 新增团购档位
- `POST   /api/merchant/config/group-pools/{id}/enable`
- `POST   /api/merchant/config/group-pools/{id}/disable`
- `DELETE /api/merchant/config/group-pools/{id}`

#### 权重与抵扣
- `GET  /api/merchant/config/weights` — 读取私有池权重
- `POST /api/merchant/config/weights` — 保存私有池权重
- `GET  /api/merchant/config/group-pool-weights` — 团购大类权重
- `POST /api/merchant/config/group-pool-weights` — 保存团购大类权重
- `GET  /api/merchant/config/deduct-config` — 抵扣配置
- `POST /api/merchant/config/deduct-config` — 保存抵扣配置（percent/dailyCap/mode）

#### 二维码
- `POST /api/merchant/config/qr-svg` — 生成二维码 SVG（请求体含内容）
- `POST /api/merchant/config/upload-qr` — 上传商家收款码图片

#### 报表
- `GET /api/merchant/config/balance-flows` — 余额流水
- `GET /api/merchant/config/balance-flows/export`
- `GET /api/merchant/config/group-records` — 团购登记记录
- `GET /api/merchant/config/group-records/export`
- `GET /api/merchant/config/external-coupons` — 外来券（外来客）
- `GET /api/merchant/config/external-coupons/export`
- `GET /api/merchant/config/ibigou-recon` — 宜必购对账
- `GET /api/merchant/config/ibigou-recon/export`
- `GET /api/merchant/config/group-qr` — 团购二维码

#### 鉴权约束
- 商家所有 /api/merchant/** 由 MerchantAuthInterceptor 统一拦截（防漏配越权）
- 必须 header `Authorization: Bearer TK_M1001_xxx`

### 端点统计更新
- 之前：30 service / 24 repo / 24 entity / 6 controller / 9 enum / 107 端点 / 5 定时任务
- 现在：**30 service / 24 repo / 24 entity / 6 controller / 9 enum / 110 端点**（新增 3 个 admin mock 端点）/ 5 定时任务

### 端点索引补充
### AdminController V2.4 新增（mock）
- `Post /api/admin/auth/login` — 平台管理员登录（接受 account+password 或 code）
- `Get /api/admin/merchant/list` — 商家列表（需要 X-Admin-Token）
- `Get /api/admin/config` — 全局配置
- `Post /api/admin/config` — 更新全局配置
- `Get /api/admin/audit/list` — 审计日志

### 端点索引补充 - 商家 V2.4（前端 merchant/index.html 用）
详见上文"前端 V2 接口契约"段落，共约 23 个新端点（prize-pools 7 + public-pools 3 + group-pools 5 + weights/group-pool-weights/deduct-config/qr-svg/upload-qr 各 1-2 + 报表 9 + group-qr 1）


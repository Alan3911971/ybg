# 微信支付联调指引（生产商户资质已就绪）

> 更新时间：2026-08-17 | 状态：🟡 待 app_id + 公网回调配置后即可联调

## 一、已就绪的生产资质

| 项 | 值 | 存储位置 |
|---|---|---|
| 微信商户号 mch_id | `1440071102`（南京微汀网络科技有限公司） | 平台后台配置（加密存储） |
| APIv3 密钥 | 用户提供（**不落盘明文**，配置时粘贴） | 平台后台配置（AES-GCM 加密，DB 存 `enc:` 前缀） |
| API 证书 | apiclient_cert.pem / apiclient_key.pem / apiclient_cert.p12 | NAS `/volume1/Download/ybg/certs/`（目录 700 / 文件 600） |
| 证书序列号 | `598230E1FFEBADCDA744EB31739DD843460232AE` | 平台后台配置 |
| 证书有效期 | 2026-08-01 ~ 2031-07-31（5 年） | — |
| 证书校验 | 商户号匹配 ✓ 公私钥 SHA1 配对一致 ✓ | — |

## 二、⏳ 待用户提供

1. **app_id（JSAPI 支付必需）**：微信支付 JSAPI 需绑定公众号（服务号）或小程序 AppID。
   请提供：公众号/小程序 AppID（wx 开头 18 位）。
2. **回调域名确认**：生产回调统一走 `https://ybgtc.com`（需在微信商户平台配置支付授权目录/回调域名）。

## 三、生产配置步骤（平台后台 UI，禁止 SSH 直插密钥）

平台后台「全局参数」Tab 配置以下 key（自动 AES-GCM 加密存储、列表脱敏）：

```
wx_pay_enabled     = 1        （0=mock 测试 / 1=真实支付）
wx_pay_mch_id      = 1440071102
wx_pay_app_id      = <待提供>
wx_pay_api_key     = <商户 API 密钥（v2，如使用）>
wx_pay_api_v3_key  = <用户提供的 APIV3 密钥>
wx_pay_cert_path   = /etc/ibigou/certs/apiclient_key.pem   （生产服务器证书路径）
wx_pay_cert_serial = 598230E1FFEBADCDA744EB31739DD843460232AE
```

> 生产部署时把 NAS `/volume1/Download/ybg/certs/` 三个证书文件复制到生产服务器受限路径
> （如 `/etc/ibigou/certs/`，权限 600），`cert_path` 指向其中的 `apiclient_key.pem`。

## 四、联调步骤

1. **测试环境（当前）**：`wx_pay_enabled=0` mock 模式已通（测试确认支付接口，生产禁）。
2. **生产切换**：提供 app_id + 部署生产 → 平台后台配置上述 key → 商家续费走真实微信支付。
3. **验证**：真实支付一笔商家年费 → 微信支付回调（PayNotifyController）→ 会员有效期叠加 → 状态已支付。
4. **安全**：密钥仅平台后台可配置（脱敏/加密），商家端无任何密钥入口；证书私钥不进代码库。

## 五、代码框架（已就绪，无需改动）

- `WxPayService`：wechatpay-java SDK，RSA 私钥 + 商户号 + 证书序列号 + APIv3 验签，配置缺失自动拦截
- `PayNotifyController`：微信支付回调（验签 → 更新会员订单状态 → 叠加有效期）
- 配置项全量在白名单，前端仅平台后台可写（脱敏显示）

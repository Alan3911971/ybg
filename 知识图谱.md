# 宜必购盲盒系统知识图谱

> 最后更新：2026-09-12（商家列表页merchants.html + 商家列表/行业接口 + G15/G16规矩 + 完整闭环）

## 一、系统架构

```
浏览器/APP(WebView) ──HTTPS──> nginx(ybgtc.com) ──> Spring Boot(ibigou.jar:8085) ──> MySQL(ybgtc.com库)
                                     │
                                     └── /h5/** 静态页面直读磁盘（wallet.html等）
                                     └── /api/** 反向代理到Java服务
```

- 后端：Java Spring Boot（com.ibigou.blindbox），Maven打包
- 前端：原生HTML/JS（h5目录），无框架
- 数据库：MySQL，库名 ybgtc.com（带点号）
- 部署：宝塔面板 + nginx + nohup

## 二、核心模块

### 1. 用户/认证
- CustomerAuthService / MerchantAuthService / AdminAuthService
- 登录接口参数 userPhone（form-urlencoded）
- 拦截器：CustomerAuthInterceptor / MerchantAuthInterceptor / AdminAuthInterceptor

### 2. 盲盒抽奖（DrawService）
- 奖池：BoxPrizePool / BoxPublicPool / BoxGroupPrizePool / BoxGroupPoolConfig
- 概率控制：BoxPrizeLimitStat、DailyDeductQuota、LimitCycle/LimitScope
- 动态二维码：DynamicQrService / IdentityQrService

### 3. 商城订单（IbigouService）
- IbigouGoods / IbigouOrder
- 下单成功 → announceService.record() 创建商家播报事件
- 优惠券：CouponService / UserCoupon
- 余额：BalanceService / UserBalanceFlow / WalletService / MemberWallet

### 4. 线下订单（OfflineOrderService）
- confirmPaid 已含播报逻辑

### 5. 支付（微信V2 + 支付宝，API/静态码双模式）
- WxPayService（V2：mch_id 1440071102，app_id wx05a588674344ed25，APIKey V2）
  - unifiedOrder(outTradeNo,金额,描述,notifyUrl,tradeType,sceneInfo)：NATIVE/MWEB 共用下单
  - nativePay() → code_url（动态二维码）；h5Pay() → mweb_url（H5拉起，G44新增）
- AlipayService（app_id 2021002144664045）
  - precreate() → qr_code；wapPay() → 收银台form HTML（AlipayTradeWapPayRequest+pageExecute，G44新增）
- CustomerPayController.createPay（下单）：**scene 参数**——wechat+mweb → mwebUrl（H5失败自动降级 native 返回 qrCode+fallbackNative）；alipay+wap → wapForm；**wechat+jsapi（微信内，G46）→ jsapi 参数（chooseWXPay 全套，失败降级 native 二维码）**；不带 scene 走二维码
- **JSAPI 微信内支付（G46）**：WxPayService.jsapiPay（trade_type=JSAPI+openid）+ OAuth2 snsapi_base 静默授权（oauthUrl/oauthOpenId）+ accessToken/jsapiTicket 内存缓存 + jssdkConfig sha1Hex 签名；WxOAuthController（新，/api/pay/wx/oauth-url、/oauth-callback、/jssdk-config）；pay.js 微信内（isInWeChat）点微信→静默授权拿 openid（sessionStorage）→createPay(scene=jsapi)→launchJsapi（wx.config→wx.chooseWXPay）；pay.html 引 jweixin-1.6.0.js；公众号域名验证文件 MP_verify_vQFrz5KHyiEjou3Q.txt（nginx 磁盘直读，公网200）
- pay.js（nginx 磁盘直读）：点击支付按渠道传 scene；mwebUrl→location.href 拉起、wapForm→form.submit() 拉起、jsapi→wx.chooseWXPay 拉起；3处 fetch 带 X-User-Token（G43修复）
- PayNotifyController（回调）：微信 /api/pay/wx/notify（V2 XML验签）、支付宝 /api/pay/alipay/notify（RSA2验签）→ OfflineOrderService.confirmPaid（OFF-前缀）
- 配置存 sys_global_config：wx_pay_enabled、wx_pay_mch_id、wx_pay_app_id、wx_pay_api_key、alipay_enabled、alipay_app_id、customer_pay_mode（0=静态码/1=API）
- 状态：支付宝WAP拉起✅（跳 mclient.alipay.com 收银台）；微信MWEB需商户平台开通H5支付（未开通自动降级动态二维码）

### 6. 语音播报（AnnounceService）
- 表 announce_log（announce_id/merchant_no/event_type/content/create_time）
- 商家端轮询：/api/merchant/announce/poll?afterId=xxx（X-Merchant-Token头）
- 前端：merchant/index.html 的 pollAnnounce / btSpeak
- TTS：/api/tts?lan=zh&text=xxx&spd=5&source=web（百度女声MP3）
- 待解决：APP WebView无声音（需Android壳原生TTS桥接）

### 7. 会员管理（merchant 后台"概览"后新增 tab，G35-G42 系列）
- 入口：merchant/index.html 会员管理 tab（切换条：会员资料/预约/回访）
- 会员资料：姓名/电话/男女/生日（含录入员工）/会员类型（自定义，V13表）/客户喜好+家人喜好（**多选标签库**，录入一次后续可复用）/赠送记录
- 回访：回访记录+明细弹窗；回访时可**赠送商品库任意商品**（店长生日/节日礼品），记录入档
- 预约：预约记录+明细弹窗；预约提醒
- 相关表：member_profile（会员资料：姓名/电话/性别/生日/录入员工/喜好标签）、merchant_member_type（商家会员类型）、member_visit（回访）、member_appointment（预约）、member_gift（赠送礼品）
- 服务：MemberService

### 8. 其他
- ChatService（聊天）、MessageService（消息）、ReportService（报表）、JobService（定时任务）
- ConfigCryptoService（配置加密）、AlertService（告警）、AuditLogService（审计）

## 三、备份体系（G45）
- GitHub：Alan3911971/ybg master（github.com 443 直连不通→用 api.github.com Git Data API 增量推送，_push_inc.py 最稳）
- NAS：Synology 192.168.31.228 /volume1/docker/github-backup/ybg-master（ssh 管道传 tar 202MB/35s，scp 不可用）
- 本地：D:\workspace-java\ybg-master（工作目录，无 .git）

## 三、H5页面结构

```
h5/customer/  ├─ index.html 抽盒（4tab：抽盒/商城/订单/我的）
              ├─ ibigou.html 商城（订单区已移除）
              ├─ order-detail.html 订单详情（独立页，深色渐变+卡片）
              ├─ my-orders.html 我的订单（独立页，统计栏+卡片，无退款按钮）
              ├─ wallet.html 我的（优惠券/余额）
              ├─ pay.html 支付页（顾客端播报已取消）
              └─ pay.js（speak/speakPay已注释）
h5/merchant/   └─ index.html 商家后台（蓝牙打印+语音播报测试）
h5/admin/      └─ index.html 平台后台（微信/支付宝配置+支付模式选择）
```

## 四、数据库关键表

| 表 | 用途 | 备注 |
|----|------|------|
| merchant | 商家 | merchant_no 唯一 |
| user_account | 用户 | |
| ibigou_goods / ibigou_order | 商城商品/订单 | |
| offline_order | 线下订单 | |
| user_coupon | 用户优惠券 | |
| announce_log | 播报事件 | |
| sys_global_config | 全局配置 | config_value=TEXT |

## 五、待办/未决

1. **JSAPI 微信内支付收尾（G46 代码已上线）**：① 用户提供公众号 AppSecret → 配置 sys_global_config.wx_pay_app_secret；② 商户平台开通 JSAPI支付并绑定公众号 wx05a588674344ed25；③ 公众号后台网页授权域名+JS接口安全域名填 ybgtc.com（验证文件已 200 就绪）；④ 配置后手机微信内实测拉起+回调入账
2. 微信 H5 支付产品开通（pay.weixin.qq.com → 产品中心 → H5支付，商户号1440071102；开通后非微信浏览器 MWEB 自动拉起，代码已就绪）
3. 手机端支付宝真实支付闭环测试（桌面无法付款；用户手机走：顾客端→到店结算→支付宝→收银台→付款→回调验证；WAP 拉起已实测成功）
4. 多商户支付方案未选型（公共小程序+支付到每商户）：服务商模式/代收+分账——用户明确先做普通支付，分账后续
5. APP商家端播报：顾客端声音已播到商户；APP WebView 无声音问题已在历史轮次处理，仍待最终确认
6. NAS 备份含 _restore/ 临时恢复文件（约150MB），可考虑精简（未确认是否清理）


## 六、2026-09-09 顾客端闭环 + 限次取消 + 测试支付锁定（最新）

### 本次修改/新增
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 开盒每日限次取消 | DrawService.checkDailyParticipation 改 return（不再 countByMerchantNoAndUserPhoneAndCreateTimeAfter 拦截） | backend/.../service/DrawService.java | ✅ 已部署（新jar运行中，连续开盒 code=0） |
| 测试支付 0.01 锁定 | 确认下单时 amt>0.01 拦截+toast（防误扣大额） | h5/customer/pay.js（btnOrder 点击） | ✅ 已部署（27434字符），验收后移除 |
| 开奖误判修复 | draw.js：仅 msg 含"登录"跳登录页；含"参与"alert | h5/customer/draw.js | ✅ 已部署 |

### 流程链路（已跑通）
顾客登录 → choose 选本店消费 → 开盒 drawNormal（不限次）→ 中奖入钱包 → pay.html 输金额 → 抵扣计算（券/余额）→ 0.01 元下单（offline/order）→ createPay 微信 native 码 → 用户手机扫码支付 0.01 元成功（订单 order_status=1）→ 闭环

### 支付模式
- 商家 pay_mode=1（API 模式）：createPay 返回 qrCode（weixin:// native），前端渲染二维码/拉起
- 微信支付 V2 密钥（商户号1440071102）、公众号"试镜舱" wx05a588674344ed25、支付宝 APPID 2021002144664045 均已配置（sys_global_config）

### 部署记录
- 当前生产 ibigou.jar（102063424B）= 含限次取消的新版，pid 1894721，端口 8085
- 备份：ibigou.jar.bak.2.1788965896（部署前旧版）
- 构建产物：/tmp/_build/backend/target/ibigou-blindbox-1.0.0-SNAPSHOT.jar

### 待办（更新）
- [ ] 验收通过后移除 pay.js 0.01 限制段，恢复真实金额支付
- [ ] 用户手机微信内 JSAPI 支付实测（公众号授权目录已配 5 个，含 ybgtc.com）
- [ ] 微信 H5 支付产品开通（非微信浏览器 MWEB 拉起）
- [ ] 多商户分账方案（用户明确后续）


## 7. 结算/支付修复（2026-09-09）

- **折扣券不生效**：后端 offline/calc、offline/order 只认用户券，不认前端开奖规则(rule) → 增加 rule 参数 + applyRule(free/discount/minus/threshold)，rule 非空不选券直接按规则计算；pay.js 传 rule
- **0.01 显示 ¥0.00**：0.01 订单被立减券/余额抵光 → pay.js calc 0.01 保底（不应用任何优惠，pay 固定 0.01）
- **微信拉起**：H5支付(mweb)未开通 → createPay 退回 native 二维码；微信内 JSAPI 自动拉起（公众号已配）；二维码用于长按识别（非扫一扫）
- **二维码样式**：容器居中(center/center)，取消扫码步骤指引，提示统一为"长按识别上方二维码完成付款"
- **验证**：calc(20, 9折)=17.24；0.01 保底 ¥0.01；native qrCode=weixin://wxpay/bizpayurl?pr=...


## 8. 奖品加载修复（2026-09-10）

- **draw.js 奖品映射**：draw/normal 接口返回奖品字段平铺在 data 下（prizeType/prizeValue/isCoupon/drawBatchNo），非嵌套 prize 对象 → 兼容 `r.data.prize || r.data`，奖品名称/emoji/id 兜底
- **pay.html script 闭合**：jweixin script 标签未闭合（缺 `></script>`），浏览器静默吞掉下一行 pay.js → 补全闭合
- **验证**：奖品自动加载显示"9.0折优惠券"；20元9.05折→实付17.34；0.01保底；微信二维码居中+长按识别提示

## 9. 顾客端首页商家信息展示（2026-09-11）

### 新增/修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| Merchant实体新增字段 | address、reservePhone、latitude、longitude、industry | entity/Merchant.java | ✅ 已部署 |
| 数据库字段 | merchant表新增 address/reserve_phone/latitude/longitude/industry（前4个已存在，industry新增） | MySQL ybgtc.com.merchant | ✅ 已执行 |
| 公开商家信息接口 | GET /api/customer/merchant/info/{merchantNo}（无需登录，直接查Merchant表） | controller/CustomerController.java | ✅ 已部署 |
| 拦截器排除 | customerAuthInterceptor排除路径新增 /api/customer/merchant/info/** | config/WebConfig.java | ✅ 已部署 |
| 首页商家信息卡片 | 行业分类标签（hero内）+ 地址/电话卡片 + 导航地图（点击跳转高德uri.amap.com） | h5/customer/index.html | ✅ 已部署 |
| 行业分类emoji映射 | 水果生鲜🍎/餐饮美食🍜/便利店🏪/美容美发💇/服饰鞋包👗/数码家电📱/教育培训📚/医疗健康🏥/休闲娱乐🎮/生活服务🔧/其他🏪 | h5/customer/index.html loadShopInfo | ✅ 已部署 |

### 接口返回示例
```json
{"code":0,"msg":"ok","data":{"merchantNo":"M90380836","merchantName":"鲜果时光生产水果店","industry":"水果生鲜","address":"南京市江宁区竹山路68号万达广场","reservePhone":"13851918986","latitude":31.9582,"longitude":118.8247}}
```

### 坑（G49-G50）
- 顾客端不要调用 /api/merchant/config/weights（需要商家token，空token报"请先登录"），要用公开接口
- CSS .shop-info-card>div 会误伤地图卡片，改用 .info-box class精确选择


## 10. 支付页0元处理（2026-09-11）

### 修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 输入0元不参与运算 | autoCalc中amt===0分支：直接设置lastResult={pay:0,...}，渲染结果，完成按钮变亮，不调用calc/后端 | h5/customer/pay.js autoCalc | ✅ 已部署 |
| 确认下单0元直接完成 | btnOrder点击时，输入0或pay<=0直接提示完成并跳转首页，不调用后端下单 | h5/customer/pay.js btnOrder click | ✅ 已部署 |
| 其他支付提示文字 | 改为"请长按二维码识别完成付款" | h5/customer/pay.js | ✅ 已部署 |

### 坑（G51）
- 金额校验要区分"未输入"和"输入0"，0是合法金额（免单/全额抵扣）
- 原逻辑 amt<=0 直接return导致输入0无法完成，改为 amt<0 拦截 + amt===0 单独处理


## 11. Spec Kit 规格驱动开发（2026-09-11，G14规矩）

### 安装
- 工具：specify-cli v1.0.7.dev0（GitHub官方 Spec Kit）
- 前置：uv 0.12.12
- 安装：uv tool install specify-cli → C:\Users\Administrator\.local\bin\specify.exe
- PATH：已用 setx 永久添加到用户环境变量

### 项目初始化
- 路径：D:\workspace-java\ybg-master
- 命令：specify init --here --force --non-interactive --integration copilot
- 生成：.github/skills/（10个speckit-* skills）+ .specify/（模板配置）

### 可用命令（Copilot中用 $speckit-xxx 调用）
| 命令 | 用途 |
|------|------|
| $speckit-constitution | 建立项目原则 |
| $speckit-specify | 定义需求规格 |
| $speckit-clarify | 澄清模糊需求 |
| $speckit-plan | 技术实现计划 |
| $speckit-tasks | 拆分任务清单 |
| $speckit-implement | 执行实现 |
| $speckit-converge | 验证实现与规格一致 |
| $speckit-analyze | 跨文档一致性分析 |
| $speckit-checklist | 质量检查清单 |
| $speckit-taskstoissues | 任务转GitHub Issues |

### 规矩（G14）
- 所有新功能开发必须走 Spec Kit 7步流程
- 紧急bug修复可跳过，但必须补采坑日志
- .specify/ 和 .github/skills/ 纳入双向备份范围

## 12. 商家列表页（2026-09-12）

### 新增/修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 商家列表页 | merchants.html：顶部搜索框(300ms防抖)+行业筛选横向滚动+商家抽屉卡片 | h5/customer/merchants.html | ✅ 已部署 |
| 商家列表接口 | GET /api/customer/merchant/list?keyword=&industry=&page=&size= | controller/CustomerController.java | ✅ 已部署 |
| 行业分类接口 | GET /api/customer/merchant/industries | controller/CustomerController.java | ✅ 已部署 |
| 拦截器排除 | customerAuthInterceptor排除 /api/customer/merchant/list、/api/customer/merchant/industries | config/WebConfig.java | ✅ 已部署 |
| 首页入口 | 紫色渐变卡片"宜必购便民生活圈-查看全部合作商家"，跳merchants.html | h5/customer/index.html | ✅ 已部署 |
| nginx配置 | location = /h5/customer/merchants.html 直读磁盘 | nginx/ybgtc.com.conf | ✅ 已重载 |

### 商家卡片结构
- 顶部：商家名称(emoji+名称) + 行业标签
- 中间：📍地址、📞电话
- 底部三个按钮：🗺️导航(跳转高德uri.amap.com)、📞电话(tel:)、进入店铺→(跳index.html?merchantNo=xxx)

### 接口返回示例
```json
// /api/customer/merchant/list
{"code":0,"msg":"ok","data":{"total":1,"page":1,"size":50,"list":[{"merchantNo":"M90380836","merchantName":"鲜果时光生产水果店","industry":"水果生鲜","address":"...","reservePhone":"...","latitude":31.9582,"longitude":118.8247}]}}

// /api/customer/merchant/industries
{"code":0,"msg":"ok","data":["水果生鲜"]}
```

### 待优化
- 商家列表接口目前用findAll()内存过滤，商家数量多时需改数据库查询+分页
- 导航地图目前是占位图+点击跳转高德，后续可接高德静态地图API显示真实地图


## 13. 规矩体系（2026-09-12 更新）

| 规矩 | 内容 | 状态 |
|------|------|------|
| G11 | 双向备份铁律（GitHub+NAS） | ✅ 执行中 |
| G12 | 四文档强制更新（规矩/共享记忆/采坑/知识图谱） | ✅ 执行中 |
| G13 | 反迎合型工作原则（6条） | ✅ 执行中 |
| G14 | Spec Kit规格驱动开发（7步流程） | ✅ 已安装初始化 |
| G15 | 演示验证与主动修复（改完必须演示，发现bug主动修） | ✅ 新增 |
| G16 | 完整闭环工作规范（8步流程，缺一不可） | ✅ 新增 |

### G16 完整闭环8步
需求理解→代码修改→构建部署→测试验证→演示给用户→主动修复bug→四文档更新→双向备份

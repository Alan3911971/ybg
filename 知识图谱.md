# 宜必购盲盒系统知识图谱

> 最后更新：2026-09-18（业主在线缴纳物业费闭环 + 支付侧两个现存bug修复）

## 一、系统架构

```
浏览器/APP(WebView) ──HTTPS──> nginx(ybgtc.com) ──> Spring Boot(ibigou.jar:8085) ──> MySQL(ybgtc.com库)
                                     │
                                     └── /h5/** 静态页面直读磁盘（wallet.html等）
                                     └── /api/** 反向代理到Java服务
```

- 后端：Java Spring Boot（com.ibigou.blindbox），Maven打包
- 前端：原生HTML/JS（h5目录），无框架
- 数据库：MySQL，库名 ybgtc.com（带点号）
- 部署：宝塔面板 + nginx + nohup

## 二、核心模块

### 1. 用户/认证
- CustomerAuthService / MerchantAuthService / AdminAuthService
- 登录接口参数 userPhone（form-urlencoded）
- 拦截器：CustomerAuthInterceptor / MerchantAuthInterceptor / AdminAuthInterceptor

### 2. 盲盒抽奖（DrawService）
- 奖池：BoxPrizePool / BoxPublicPool / BoxGroupPrizePool / BoxGroupPoolConfig
- 概率控制：BoxPrizeLimitStat、DailyDeductQuota、LimitCycle/LimitScope
- 动态二维码：DynamicQrService / IdentityQrService

### 3. 商城订单（IbigouService）
- IbigouGoods / IbigouOrder
- 下单成功 → announceService.record() 创建商家播报事件
- 优惠券：CouponService / UserCoupon
- 余额：BalanceService / UserBalanceFlow / WalletService / MemberWallet

### 4. 线下订单（OfflineOrderService）
- confirmPaid 已含播报逻辑

### 5. 支付（微信V2 + 支付宝，API/静态码双模式）
- WxPayService（V2：mch_id 1440071102，app_id wx05a588674344ed25，APIKey V2）
  - unifiedOrder(outTradeNo,金额,描述,notifyUrl,tradeType,sceneInfo)：NATIVE/MWEB 共用下单
  - nativePay() → code_url（动态二维码）；h5Pay() → mweb_url（H5拉起，G44新增）
- AlipayService（app_id 2021002144664045）
  - precreate() → qr_code；wapPay() → 收银台form HTML（AlipayTradeWapPayRequest+pageExecute，G44新增）
- CustomerPayController.createPay（下单）：**scene 参数**——wechat+mweb → mwebUrl（H5失败自动降级 native 返回 qrCode+fallbackNative）；alipay+wap → wapForm；**wechat+jsapi（微信内，G46）→ jsapi 参数（chooseWXPay 全套，失败降级 native 二维码）**；不带 scene 走二维码
- **JSAPI 微信内支付（G46）**：WxPayService.jsapiPay（trade_type=JSAPI+openid）+ OAuth2 snsapi_base 静默授权（oauthUrl/oauthOpenId）+ accessToken/jsapiTicket 内存缓存 + jssdkConfig sha1Hex 签名；WxOAuthController（新，/api/pay/wx/oauth-url、/oauth-callback、/jssdk-config）；pay.js 微信内（isInWeChat）点微信→静默授权拿 openid（sessionStorage）→createPay(scene=jsapi)→launchJsapi（wx.config→wx.chooseWXPay）；pay.html 引 jweixin-1.6.0.js；公众号域名验证文件 MP_verify_vQFrz5KHyiEjou3Q.txt（nginx 磁盘直读，公网200）
- pay.js（nginx 磁盘直读）：点击支付按渠道传 scene；mwebUrl→location.href 拉起、wapForm→form.submit() 拉起、jsapi→wx.chooseWXPay 拉起；3处 fetch 带 X-User-Token（G43修复）
- PayNotifyController（回调）：微信 /api/pay/wx/notify（V2 XML验签）、支付宝 /api/pay/alipay/notify（RSA2验签）→ OfflineOrderService.confirmPaid（OFF-前缀）
- 配置存 sys_global_config：wx_pay_enabled、wx_pay_mch_id、wx_pay_app_id、wx_pay_api_key、alipay_enabled、alipay_app_id、customer_pay_mode（0=静态码/1=API）
- 状态：支付宝WAP拉起✅（跳 mclient.alipay.com 收银台）；微信MWEB需商户平台开通H5支付（未开通自动降级动态二维码）

### 6. 语音播报（AnnounceService）
- 表 announce_log（announce_id/merchant_no/event_type/content/create_time）
- 商家端轮询：/api/merchant/announce/poll?afterId=xxx（X-Merchant-Token头）
- 前端：merchant/index.html 的 pollAnnounce / btSpeak
- TTS：/api/tts?lan=zh&text=xxx&spd=5&source=web（百度女声MP3）
- 待解决：APP WebView无声音（需Android壳原生TTS桥接）

### 7. 会员管理（merchant 后台"概览"后新增 tab，G35-G42 系列）
- 入口：merchant/index.html 会员管理 tab（切换条：会员资料/预约/回访）
- 会员资料：姓名/电话/男女/生日（含录入员工）/会员类型（自定义，V13表）/客户喜好+家人喜好（**多选标签库**，录入一次后续可复用）/赠送记录
- 回访：回访记录+明细弹窗；回访时可**赠送商品库任意商品**（店长生日/节日礼品），记录入档
- 预约：预约记录+明细弹窗；预约提醒
- 相关表：member_profile（会员资料：姓名/电话/性别/生日/录入员工/喜好标签）、merchant_member_type（商家会员类型）、member_visit（回访）、member_appointment（预约）、member_gift（赠送礼品）
- 服务：MemberService

### 8. 其他
- ChatService（聊天）、MessageService（消息）、ReportService（报表）、JobService（定时任务）
- ConfigCryptoService（配置加密）、AlertService（告警）、AuditLogService（审计）

## 三、备份体系（G45）
- GitHub：Alan3911971/ybg master（github.com 443 直连不通→用 api.github.com Git Data API 增量推送，_push_inc.py 最稳）
- NAS：Synology 192.168.31.228 /volume1/docker/github-backup/ybg-master（ssh 管道传 tar 202MB/35s，scp 不可用）
- 本地：D:\workspace-java\ybg-master（工作目录，无 .git）

## 三、H5页面结构

```
h5/customer/  ├─ index.html 抽盒（4tab：抽盒/商城/订单/我的）
              ├─ ibigou.html 商城（订单区已移除）
              ├─ order-detail.html 订单详情（独立页，深色渐变+卡片）
              ├─ my-orders.html 我的订单（独立页，统计栏+卡片，无退款按钮）
              ├─ wallet.html 我的（优惠券/余额）
              ├─ pay.html 支付页（顾客端播报已取消）
              └─ pay.js（speak/speakPay已注释）
h5/merchant/   └─ index.html 商家后台（蓝牙打印+语音播报测试）
h5/admin/      └─ index.html 平台后台（微信/支付宝配置+支付模式选择）
```

## 四、数据库关键表

| 表 | 用途 | 备注 |
|----|------|------|
| merchant | 商家 | merchant_no 唯一 |
| user_account | 用户 | |
| ibigou_goods / ibigou_order | 商城商品/订单 | |
| offline_order | 线下订单 | |
| user_coupon | 用户优惠券 | |
| announce_log | 播报事件 | |
| sys_global_config | 全局配置 | config_value=TEXT |

## 五、待办/未决

1. **JSAPI 微信内支付收尾（G46 代码已上线）**：① 用户提供公众号 AppSecret → 配置 sys_global_config.wx_pay_app_secret；② 商户平台开通 JSAPI支付并绑定公众号 wx05a588674344ed25；③ 公众号后台网页授权域名+JS接口安全域名填 ybgtc.com（验证文件已 200 就绪）；④ 配置后手机微信内实测拉起+回调入账
2. 微信 H5 支付产品开通（pay.weixin.qq.com → 产品中心 → H5支付，商户号1440071102；开通后非微信浏览器 MWEB 自动拉起，代码已就绪）
3. 手机端支付宝真实支付闭环测试（桌面无法付款；用户手机走：顾客端→到店结算→支付宝→收银台→付款→回调验证；WAP 拉起已实测成功）
4. 多商户支付方案未选型（公共小程序+支付到每商户）：服务商模式/代收+分账——用户明确先做普通支付，分账后续
5. APP商家端播报：顾客端声音已播到商户；APP WebView 无声音问题已在历史轮次处理，仍待最终确认
6. NAS 备份含 _restore/ 临时恢复文件（约150MB），可考虑精简（未确认是否清理）


## 六、2026-09-09 顾客端闭环 + 限次取消 + 测试支付锁定（最新）

### 本次修改/新增
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 开盒每日限次取消 | DrawService.checkDailyParticipation 改 return（不再 countByMerchantNoAndUserPhoneAndCreateTimeAfter 拦截） | backend/.../service/DrawService.java | ✅ 已部署（新jar运行中，连续开盒 code=0） |
| 测试支付 0.01 锁定 | 确认下单时 amt>0.01 拦截+toast（防误扣大额） | h5/customer/pay.js（btnOrder 点击） | ✅ 已部署（27434字符），验收后移除 |
| 开奖误判修复 | draw.js：仅 msg 含"登录"跳登录页；含"参与"alert | h5/customer/draw.js | ✅ 已部署 |

### 流程链路（已跑通）
顾客登录 → choose 选本店消费 → 开盒 drawNormal（不限次）→ 中奖入钱包 → pay.html 输金额 → 抵扣计算（券/余额）→ 0.01 元下单（offline/order）→ createPay 微信 native 码 → 用户手机扫码支付 0.01 元成功（订单 order_status=1）→ 闭环

### 支付模式
- 商家 pay_mode=1（API 模式）：createPay 返回 qrCode（weixin:// native），前端渲染二维码/拉起
- 微信支付 V2 密钥（商户号1440071102）、公众号"试镜舱" wx05a588674344ed25、支付宝 APPID 2021002144664045 均已配置（sys_global_config）

### 部署记录
- 当前生产 ibigou.jar（102063424B）= 含限次取消的新版，pid 1894721，端口 8085
- 备份：ibigou.jar.bak.2.1788965896（部署前旧版）
- 构建产物：/tmp/_build/backend/target/ibigou-blindbox-1.0.0-SNAPSHOT.jar

### 待办（更新）
- [ ] 验收通过后移除 pay.js 0.01 限制段，恢复真实金额支付
- [ ] 用户手机微信内 JSAPI 支付实测（公众号授权目录已配 5 个，含 ybgtc.com）
- [ ] 微信 H5 支付产品开通（非微信浏览器 MWEB 拉起）
- [ ] 多商户分账方案（用户明确后续）


## 7. 结算/支付修复（2026-09-09）

- **折扣券不生效**：后端 offline/calc、offline/order 只认用户券，不认前端开奖规则(rule) → 增加 rule 参数 + applyRule(free/discount/minus/threshold)，rule 非空不选券直接按规则计算；pay.js 传 rule
- **0.01 显示 ¥0.00**：0.01 订单被立减券/余额抵光 → pay.js calc 0.01 保底（不应用任何优惠，pay 固定 0.01）
- **微信拉起**：H5支付(mweb)未开通 → createPay 退回 native 二维码；微信内 JSAPI 自动拉起（公众号已配）；二维码用于长按识别（非扫一扫）
- **二维码样式**：容器居中(center/center)，取消扫码步骤指引，提示统一为"长按识别上方二维码完成付款"
- **验证**：calc(20, 9折)=17.24；0.01 保底 ¥0.01；native qrCode=weixin://wxpay/bizpayurl?pr=...


## 8. 奖品加载修复（2026-09-10）

- **draw.js 奖品映射**：draw/normal 接口返回奖品字段平铺在 data 下（prizeType/prizeValue/isCoupon/drawBatchNo），非嵌套 prize 对象 → 兼容 `r.data.prize || r.data`，奖品名称/emoji/id 兜底
- **pay.html script 闭合**：jweixin script 标签未闭合（缺 `></script>`），浏览器静默吞掉下一行 pay.js → 补全闭合
- **验证**：奖品自动加载显示"9.0折优惠券"；20元9.05折→实付17.34；0.01保底；微信二维码居中+长按识别提示

## 9. 顾客端首页商家信息展示（2026-09-11）

### 新增/修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| Merchant实体新增字段 | address、reservePhone、latitude、longitude、industry | entity/Merchant.java | ✅ 已部署 |
| 数据库字段 | merchant表新增 address/reserve_phone/latitude/longitude/industry（前4个已存在，industry新增） | MySQL ybgtc.com.merchant | ✅ 已执行 |
| 公开商家信息接口 | GET /api/customer/merchant/info/{merchantNo}（无需登录，直接查Merchant表） | controller/CustomerController.java | ✅ 已部署 |
| 拦截器排除 | customerAuthInterceptor排除路径新增 /api/customer/merchant/info/** | config/WebConfig.java | ✅ 已部署 |
| 首页商家信息卡片 | 行业分类标签（hero内）+ 地址/电话卡片 + 导航地图（点击跳转高德uri.amap.com） | h5/customer/index.html | ✅ 已部署 |
| 行业分类emoji映射 | 水果生鲜🍎/餐饮美食🍜/便利店🏪/美容美发💇/服饰鞋包👗/数码家电📱/教育培训📚/医疗健康🏥/休闲娱乐🎮/生活服务🔧/其他🏪 | h5/customer/index.html loadShopInfo | ✅ 已部署 |

### 接口返回示例
```json
{"code":0,"msg":"ok","data":{"merchantNo":"M90380836","merchantName":"鲜果时光生产水果店","industry":"水果生鲜","address":"南京市江宁区竹山路68号万达广场","reservePhone":"13851918986","latitude":31.9582,"longitude":118.8247}}
```

### 坑（G49-G50）
- 顾客端不要调用 /api/merchant/config/weights（需要商家token，空token报"请先登录"），要用公开接口
- CSS .shop-info-card>div 会误伤地图卡片，改用 .info-box class精确选择


## 10. 支付页0元处理（2026-09-11）

### 修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 输入0元不参与运算 | autoCalc中amt===0分支：直接设置lastResult={pay:0,...}，渲染结果，完成按钮变亮，不调用calc/后端 | h5/customer/pay.js autoCalc | ✅ 已部署 |
| 确认下单0元直接完成 | btnOrder点击时，输入0或pay<=0直接提示完成并跳转首页，不调用后端下单 | h5/customer/pay.js btnOrder click | ✅ 已部署 |
| 其他支付提示文字 | 改为"请长按二维码识别完成付款" | h5/customer/pay.js | ✅ 已部署 |

### 坑（G51）
- 金额校验要区分"未输入"和"输入0"，0是合法金额（免单/全额抵扣）
- 原逻辑 amt<=0 直接return导致输入0无法完成，改为 amt<0 拦截 + amt===0 单独处理


## 11. Spec Kit 规格驱动开发（2026-09-11，G14规矩）

### 安装
- 工具：specify-cli v1.0.7.dev0（GitHub官方 Spec Kit）
- 前置：uv 0.12.12
- 安装：uv tool install specify-cli → C:\Users\Administrator\.local\bin\specify.exe
- PATH：已用 setx 永久添加到用户环境变量

### 项目初始化
- 路径：D:\workspace-java\ybg-master
- 命令：specify init --here --force --non-interactive --integration copilot
- 生成：.github/skills/（10个speckit-* skills）+ .specify/（模板配置）

### 可用命令（Copilot中用 $speckit-xxx 调用）
| 命令 | 用途 |
|------|------|
| $speckit-constitution | 建立项目原则 |
| $speckit-specify | 定义需求规格 |
| $speckit-clarify | 澄清模糊需求 |
| $speckit-plan | 技术实现计划 |
| $speckit-tasks | 拆分任务清单 |
| $speckit-implement | 执行实现 |
| $speckit-converge | 验证实现与规格一致 |
| $speckit-analyze | 跨文档一致性分析 |
| $speckit-checklist | 质量检查清单 |
| $speckit-taskstoissues | 任务转GitHub Issues |

### 规矩（G14）
- 所有新功能开发必须走 Spec Kit 7步流程
- 紧急bug修复可跳过，但必须补采坑日志
- .specify/ 和 .github/skills/ 纳入双向备份范围

## 12. 商家列表页（2026-09-12）

### 新增/修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 商家列表页 | merchants.html：顶部搜索框(300ms防抖)+行业筛选横向滚动+商家抽屉卡片 | h5/customer/merchants.html | ✅ 已部署 |
| 商家列表接口 | GET /api/customer/merchant/list?keyword=&industry=&page=&size= | controller/CustomerController.java | ✅ 已部署 |
| 行业分类接口 | GET /api/customer/merchant/industries | controller/CustomerController.java | ✅ 已部署 |
| 拦截器排除 | customerAuthInterceptor排除 /api/customer/merchant/list、/api/customer/merchant/industries | config/WebConfig.java | ✅ 已部署 |
| 首页入口 | 紫色渐变卡片"宜必购便民生活圈-查看全部合作商家"，跳merchants.html | h5/customer/index.html | ✅ 已部署 |
| nginx配置 | location = /h5/customer/merchants.html 直读磁盘 | nginx/ybgtc.com.conf | ✅ 已重载 |

### 商家卡片结构
- 顶部：商家名称(emoji+名称) + 行业标签
- 中间：📍地址、📞电话
- 底部三个按钮：🗺️导航(跳转高德uri.amap.com)、📞电话(tel:)、进入店铺→(跳index.html?merchantNo=xxx)

### 接口返回示例
```json
// /api/customer/merchant/list
{"code":0,"msg":"ok","data":{"total":1,"page":1,"size":50,"list":[{"merchantNo":"M90380836","merchantName":"鲜果时光生产水果店","industry":"水果生鲜","address":"...","reservePhone":"...","latitude":31.9582,"longitude":118.8247}]}}

// /api/customer/merchant/industries
{"code":0,"msg":"ok","data":["水果生鲜"]}
```

### 待优化
- 商家列表接口目前用findAll()内存过滤，商家数量多时需改数据库查询+分页
- 导航地图目前是占位图+点击跳转高德，后续可接高德静态地图API显示真实地图


## 13. 规矩体系（2026-09-12 更新）

| 规矩 | 内容 | 状态 |
|------|------|------|
| G11 | 双向备份铁律（GitHub+NAS） | ✅ 执行中 |
| G12 | 四文档强制更新（规矩/共享记忆/采坑/知识图谱） | ✅ 执行中 |
| G13 | 反迎合型工作原则（6条） | ✅ 执行中 |
| G14 | Spec Kit规格驱动开发（7步流程） | ✅ 已安装初始化 |
| G15 | 演示验证与主动修复（改完必须演示，发现bug主动修） | ✅ 新增 |
| G16 | 完整闭环工作规范（8步流程，缺一不可） | ✅ 新增 |

### G16 完整闭环8步
需求理解→代码修改→构建部署→测试验证→演示给用户→主动修复bug→四文档更新→双向备份

## 14. 商家后台店铺信息编辑（2026-09-13）

### 新增/修改
| 项 | 内容 | 位置 | 状态 |
|----|------|------|------|
| 店铺信息接口 | POST /api/merchant/config/shop-info | controller/MerchantConfigController.java | ✅ 已部署 |
| 商家信息编辑表单 | 设置页(p8)新增行业分类/地址/预约电话/纬度/经度 | h5/merchant/index.html | ✅ 已部署 |
| 自动加载 | loadMerchantConfig调用/api/merchant/config/weights填充表单 | h5/merchant/index.html | ✅ 已部署 |
| 双写保存 | 保存按钮先存localStorage再调后端接口 | h5/merchant/index.html | ✅ 已部署 |

### 接口参数
```
POST /api/merchant/config/shop-info
Headers: X-Merchant-Token
FormData: merchantName, address, reservePhone, latitude, longitude, industry
（所有参数required=false，只更新传入的字段）
返回: {code:0, msg:"ok"}
```

### 表单字段
- 商家名称（原有）
- 联系电话（原有）
- 行业分类（新增）→ 顾客端首页industry-tag、商家列表页行业筛选
- 店铺地址（新增）→ 顾客端首页shop-info-card、商家列表页地址显示
- 预约电话（新增）→ 顾客端首页电话、商家列表页电话按钮
- 纬度（新增，选填）→ 导航地图
- 经度（新增，选填）→ 导航地图
- 退款说明（原有）

### 数据流
商家后台编辑 → POST /api/merchant/config/shop-info → merchant表更新 → 顾客端GET /api/customer/merchant/info/{no} / /list 读取展示

## 15. 商家列表接口性能优化与安全修复（2026-09-13）

### 性能优化
| 项 | 优化前 | 优化后 |
|----|--------|--------|
| 商家列表 | merchantRepository.findAll() 内存过滤+内存分页 | @Query findActiveMerchants 数据库分页查询 |
| 行业分类 | findAll() 内存stream去重排序 | @Query findDistinctActiveIndustries 数据库DISTINCT查询 |

### Repository新增方法
```java
// 分页查询启用商家，支持关键词(名称/地址/行业)和行业筛选
@Query("SELECT m FROM Merchant m WHERE m.status = 1 " +
       "AND (:keyword IS NULL OR :keyword = '' OR m.merchantName LIKE %:keyword% OR m.address LIKE %:keyword% OR m.industry LIKE %:keyword%) " +
       "AND (:industry IS NULL OR :industry = '' OR m.industry = :industry) " +
       "ORDER BY m.createTime DESC")
Page<Merchant> findActiveMerchants(String keyword, String industry, Pageable pageable);

// 获取所有启用商家的行业分类（去重排序）
@Query("SELECT DISTINCT m.industry FROM Merchant m WHERE m.status = 1 AND m.industry IS NOT NULL AND m.industry <> '' ORDER BY m.industry")
List<String> findDistinctActiveIndustries();
```

### 安全修复
- Merchant实体类 loginPwd 字段添加 @JsonIgnore
- 所有返回Merchant对象的公开接口不再泄露密码哈希
- 影响范围：/api/customer/merchant/info/{no}、/api/customer/merchant/list、/api/merchant/config/weights

### 待优化
- loginAccount（商家登录账号）也属于敏感信息，后续可考虑用DTO只返回需要的字段
- 其他实体类（如User、Member）也需要检查是否有敏感字段泄露

## 16. 导航地图真实静态地图（2026-09-13）

### 架构
```
前端 <--> /api/map/static (后端代理) <--> 高德静态地图API
                ↑
           AMAP_KEY存.env，不暴露前端
```

### 新增接口
- `GET /api/map/static?lat=xx&lng=xx&zoom=15&size=400*200`
- 返回 image/png，缓存1小时
- 无key或API失败时返回1x1透明PNG占位图

### 前端改动
- 顾客端首页 index.html：shop-map从绿色渐变占位改为img真实地图+底部渐变遮罩文字
- 商家列表页 merchants.html：商家卡片新增地图缩略图（100px高），点击跳转导航

### 技术选型踩坑
| 方案 | 结果 |
|------|------|
| OpenStreetMap静态地图 | ❌ 国内不可访问（连接超时） |
| OpenStreetMap瓦片 | ❌ 国内不可访问 |
| 高德瓦片直连 | ❌ 返回200但179字节空白图 |
| 高德静态地图API+后端代理 | ✅ 可用，需申请key |

### 待配置
- AMAP_KEY：高德开放平台申请（免费），写入服务器 `/www/wwwroot/ybgtc.com/.env`
- 申请地址：https://lbs.amap.com/ → 控制台 → 应用管理 → 创建Key（服务平台选"Web服务"）

## 17. 高德地图Key配置与首页入口优化（2026-09-13）

### 高德地图Key
- Key: 656125e49cd10e7ca28fc537eea125f6（已写入服务器.env AMAP_KEY）
- 配置效果：地图接口返回从67字节(占位图) → 15653字节(真实地图)
- 首页导航地图：460x160 真实地图
- 商家列表页地图缩略图：460x140 真实地图

### 首页入口卡片优化
- 原文字："宜必购便民生活圈" / "查看全部合作商家 →"
- 新文字："查询参与活动商家" / "按分类筛选 / 搜索商家 →"
- 点击跳转：merchants.html（商家列表页）

### 商家列表页功能清单
- 搜索框：支持商家名称、地址、行业关键词搜索（300ms防抖）
- 行业分类：横向滚动标签，全部+各行业，点击筛选
- 商家卡片：名称/行业标签/地址/电话/地图缩略图/导航按钮/电话按钮/进入店铺按钮
- 底部统计：共X家商家

## 18. 会员管理功能验收与bug修复（2026-09-13）

### 发现的bug
- 现象：商家端会员tab显示0个会员
- 根因：商家端api.js的token()函数优先返回ibigou_user_token，导致请求头是X-User-Token，而商家接口需要X-Merchant-Token
- 触发条件：商家端同时登录了顾客端（localStorage中同时存在ibigou_user_token和ibigou_merchant_token）

### 修复方案
- api.js token()函数：优先返回ibigou_merchant_token
- api.js header判断：优先使用X-Merchant-Token
- nginx新增：merchant/api.js直读磁盘配置（以后修改api.js无需重新打包jar）

### 会员管理功能清单（验收通过）
| 功能 | 状态 | 说明 |
|------|------|------|
| 会员列表 | ✅ | 按订单聚合，显示姓名/电话/性别/会员类型/消费统计 |
| 会员资料 | ✅ | 姓名/性别/会员类型/客户喜好/家人喜好/生日/备注/录入员工 |
| 客户喜好标签 | ✅ | 多选标签，从历史录入中去重 |
| 家人喜好标签 | ✅ | 多选标签，从历史录入中去重 |
| 预约记录 | ✅ | 添加预约（时间/项目/备注/提醒）+ 完成/取消 + 明细 |
| 回访记录 | ✅ | 添加回访（内容/结果/赠送商品）+ 明细 |
| 礼品赠送 | ✅ | 选择商品/数量/备注，回访时也可赠送 |
| 会员类型管理 | ✅ | 添加/删除会员类型 |
| 最近订单 | ✅ | 会员资料中显示最近订单 |

### 数据库表
- user_account：全局用户账号（user_phone主键，无merchant_no）
- member_profile：会员资料（merchant_no + user_phone关联）
- member_appointment：会员预约
- member_visit：会员回访
- member_gift：会员赠送记录
- merchant_member_type：商家会员类型

## 19. 安全加固与测试类修复（2026-09-13）

### 安全加固
- 问题：公开接口`/api/customer/merchant/info`和`/api/customer/merchant/list`返回完整Merchant对象，包含敏感字段
- 修复：Merchant实体类以下字段添加`@JsonIgnore`：
  - `loginPwd`（密码哈希，之前已有）
  - `loginAccount`（登录手机号，本次新增）
  - `broadcastToken`（播报设备token，本次新增）
- 验证：公开接口返回字段从26个减少到25个，敏感字段均不在返回中

### 测试类修复
- OrderCalcServiceTest.java：calc方法签名从4参数改为6参数（新增rule和useBalance），测试类未同步导致编译失败
- 修复：所有calc调用更新为6参数，新增`不使用余额抵扣时实付全款`测试用例
- 注意：当前构建仍用`-Dmaven.test.skip=true`跳过测试，后续应启用测试

### 安全规范（新增）
- 公开接口禁止返回完整实体对象，必须用@JsonIgnore或DTO过滤敏感字段
- 每次新增实体字段都要评估是否需要@JsonIgnore
- 敏感字段包括：密码、token、登录账号、内部配置等

## 20. 顾客端首页商家信息区域调整（2026-09-13）

### 用户需求
- 首页不直接显示商家地址/电话/地图
- 点击"查询参与活动商家"入口进去后再显示商家信息

### 修改内容
- 删除index.html中的shopInfoCard（地址+电话）HTML块
- 删除index.html中的shopMap（导航地图）HTML块
- 删除相关JS显示逻辑（loadMerchantInfo中的商家信息和地图部分）
- 保留入口卡片："查询参与活动商家 / 按分类筛选 / 搜索商家 →"，点击跳merchants.html

### 商家信息展示位置
- 商家信息在merchants.html商家列表页的商家卡片中显示
- 商家卡片包含：名称/行业标签/地址/电话/地图缩略图/导航按钮/电话按钮/进入店铺按钮
- 首页只显示：行业标签、商家名称、商家编号、入口卡片、奖池预览、盲盒按钮、统计、中奖榜

## 21. 首页布局修复与现有功能优化（2026-09-13）

### 首页布局重叠bug修复
- 问题：删除商家信息区域后，入口卡片与奖池预览重叠
- 根因：.prizes使用margin:-24px负边距（原设计与商家信息区域重叠），删除商家信息后与入口卡片重叠
- 修复：.prizes margin改为16px
- 验证：entryCardTop=274, prizesTop=354, overlap=False

### 现有功能优化检查结果
| 页面 | 功能 | 状态 |
|------|------|------|
| 商家列表页 | 搜索防抖(300ms) | ✅ 已有 |
| 商家列表页 | 行业筛选选中状态 | ✅ 已有 |
| 商家列表页 | 商家卡片(地图/导航/电话/进入店铺) | ✅ 已有 |
| 商家端 | 店铺信息编辑(名称/电话/行业/地址/经纬度) | ✅ 已有 |
| 商家端 | 会员管理(列表/资料/预约/回访/赠送) | ✅ 已有 |
| 商家端 | 收款码上传(4种支付方式) | ✅ 已有 |
| 顾客端 | 支付流程(非API/API模式) | ✅ 已有 |
| 播报 | 两段播报/播放锁/去重 | ✅ 已有 |

## 22. 中奖榜分页显示优化（2026-09-13）

### 需求
- 中奖榜显示20条太长，页面需要滚动很多
- 用户要求分页显示，默认10条数据

### 实现
- 全局变量：`_luckyAll`（完整列表）、`_luckyShown`（当前显示数量）、`_luckyPageSize=10`
- `renderLuckyList()`函数只渲染前`_luckyShown`条
- 底部添加"加载更多（还有N条）"按钮，点击增加10条
- 全部显示后隐藏按钮
- luckyHint显示"共 N 条"（之前是"近 N 条"）

### 验证
- 默认显示10条 ✅
- 点击加载更多→显示20条 ✅
- 全部显示后按钮隐藏 ✅

### 相关文件
- `backend/src/main/resources/static/h5/customer/index.html`

## 23. 蓝牙打印预览修复（2026-09-14）

### 问题
- 打印预览弹窗中商家名称被截断
- 标题和按钮显示乱码（UTF-8被错误解析为Latin-1）
- 纸张规格切换不生效
- 58mm纸张下内容被遮挡

### 根因
1. HTML/JS中有大量乱码（如"ð¨ï¸ æå°é¢è§"）
2. 默认纸张58mm太小（203×140px），二维码120px+文字总高164px超出范围
3. 事件绑定代码在DOM加载前执行，querySelectorAll找不到按钮
4. 内容大小固定，不随纸张规格调整

### 修复
1. 修复所有乱码（标题/商家名称/副标题/确认按钮/提示文字）
2. 默认纸张改为80mm（280×210px），保留58mm/80mm两种规格
3. 事件绑定移到DOMContentLoaded中
4. _updatePreview函数根据纸张规格动态调整内容大小：
   - 58mm：二维码90px，名称11px，副标题9px
   - 80mm：二维码120px，名称13px，副标题10px

### 验证
- 58mm：内容高度140px = 纸张高度，不被遮挡
- 80mm：内容高度159px < 纸张高度210px，有余量
- 纸张规格切换正常

### 相关文件
- `backend/src/main/resources/static/h5/merchant/index.html`

## 24. 打印功能入口优化（2026-09-14）

### 调整
- 取消概览页快捷打印入口（用户要求保留在设置里）
- 设置页取消"浏览器打印"和"测试打印"按钮
- 只保留：连接蓝牙设备、生成打印二维码、蓝牙打印

### 最终打印流程（APP端）
1. 商家后台 → 设置tab
2. 点"🖨️ 生成打印二维码"
3. 点"🔵 蓝牙打印"
4. 选择纸张规格（58mm/80mm），调整位置/缩放
5. 点"确认打印"

### 相关文件
- `backend/src/main/resources/static/h5/merchant/index.html`

## 25. 蓝牙打印按钮点击无反应bug修复（2026-09-14）

### 现象
点击"蓝牙打印"按钮无反应，预览弹窗不打开

### 根因
事件绑定代码在IIFE的DOMContentLoaded回调里，但回调未执行
- onclick为null，typeof null === 'object'（容易误判为已绑定）
- addEventListener重新绑定也无效（因为回调没执行）
- _showPrintPreview在IIFE内非全局，手动调用无效

### 解决方案
1. 页面底部</body>前添加独立<script>标签
2. 定义全局函数window._handleBtPrint，完整实现打印逻辑
3. 按钮HTML添加onclick="window._handleBtPrint && window._handleBtPrint()"

### 经验教训
IIFE内的DOMContentLoaded回调可能因为各种原因不执行
关键按钮事件应该用：独立script标签 + 全局函数 + 内联onclick，最可靠

### 相关文件
- `backend/src/main/resources/static/h5/merchant/index.html`

## 26. 打印预览弹窗内容位置修复（2026-09-14）

### 现象
预览弹窗打开了，但纸张区域内二维码和商家名称不显示

### 根因
独立script中的_handleBtPrint函数只设置了img.src和name.textContent，没有设置ppContent的left/top/transform
- ppContent是position:absolute，默认left:0/top:0
- 加上CSS中的transform:translate(-50%,-50%)，内容被移到纸张外面不可见

### 修复
在独立script中直接设置：
- paper宽高（80mm: 280×210px）
- content的left=pw/2, top=ph/2, transform=translate(-50%,-50%) scale(1)
- img宽高120px，name字号13px

### 经验教训
absolute定位的元素必须设置left/top，否则默认在左上角，加上translate后会跑到容器外面

### 相关文件
- `backend/src/main/resources/static/h5/merchant/index.html`

## 商家APP蓝牙打印（DP80S/LPAPI）2026-09
- **关键节点**：merchant/index.html 打印链路 = 生成二维码(btnGenPrintQr) → 蓝牙打印(_handleBtPrint 显示预览) → 确认打印(_handleConfirmPrint → AndroidPrinter.printQR LPAPI 单任务)
- **真凶**：_handleConfirmPrint 曾执行调试流程（4个print任务+alert），一次点击=4次打印+4次定位空走
- **LPAPI要点**：startJob(宽mm,高mm,角度)；setPrintPageGapType(2=间隙纸)；setPrintPageGapLength(mm)；DP80S每任务先空走~30mm定位是固件行为
- **部署**：生产H5=磁盘直放 /www/wwwroot/ybgtc.com/h5/（非jar内），改完SSH上传即可，APP WebView刷新即生效无需重装
- **陷阱**：HTML/JS自动替换后必须校验 script 标签配对；豆包翻译器会向线上页面注入 <script src=/i18n/frontend/translator.js> 干扰浏览器端调试判断

## 物业系统（2026-09-18 部署）
- 生产URL：物业后台 https://ybgtc.com/h5/property/admin/login.html （admin/Admin@2026）
- 业主端 https://ybgtc.com/h5/property/customer/login.html
- 后端接口 /api/property/admin/*、/api/property/owner/*
- 数据库：ybgtc.com 库 34张 property_* 表（snake_case 列名）
- 部署要点：nginx 需加 location ^~ /h5/property/ 磁盘直放；DDL 必须 snake_case；login fetch 必须带 Content-Type

- 【09-18 需求1】楼栋/房间接口：/api/property/admin/{communities,buildings,rooms} CRUD + /rooms/{id}/bind + /bills/generate-year（已验证48张）；前端 buildings.html
- 【09-18 需求3】业主识别接口 GET /api/property/owner/lookup?phone（免token）；顾客端首页已加「我的物业」卡片（绑定业主显示）
- 【09-18 待办】需求2 物业开盲盒收费（复用商家开盲盒流程）未实现

- 【09-18 需求2】物业后台工作台「开盲盒收款码」（qrcodejs 内联）：读 /p1/merchant-bindings → 生成 choose.html?merchantNo= 二维码 → 顾客扫码抽盲盒支付分账给物业；已验证 QRCode 渲染 + choose 页正常
- 【09-18 完成】三需求全部落地：①楼栋/房间/年度账单 ②物业开盲盒收款码 ③顾客端业主合并卡片

- 【09-18 完成】家庭成员扫码绑定闭环：family.html 二维码卡（内联 qrcodejs）→ family-bind.html 扫码落地页（填姓名/手机号/关系）→ POST bind-by-qr → lookup 识别 isFamily → 顾客端显示「业主的家庭」卡片。接口：GET /api/property/owner/family/qr-info?ownerId=、POST /api/property/owner/family/bind-by-qr。前端：property/customer/family.html、family-bind.html、customer/index.html。验证：二维码渲染、绑定李小红成功、lookup isFamily=true、成员列表3人

- 【09-18 完成】物业后台窄屏防变形：19页统一注入 @media(max-width:900px) 响应式（sidebar收起+汉堡、.main/.main-content min-width:0、.card overflow-x:auto），根因 flex item 被表格撑开。375px iframe 实测 8 页无横向溢出。buildings.html 用 .main-content 类名（与其它页 .main 不同）
- 【09-18 完成】业主在线缴纳物业费闭环：bills.html「去缴费」→ POST /bills/{id}/pay?channel=wechat|alipay（新表 property_payment，单号 PB+毫秒）→ 微信/支付宝真实二维码（api.qrserver 渲染）→ 前端 2.5s 轮询 pay-status → 回调（WxPay/Alipay handleNotify 按 PB 前缀分流）→ 账单 paid 累计、抵扣+已缴>=全额则 status=1。演示：bill 1 微信150元缴清、bills.html 已缴清/待缴费/去缴费按钮齐全

- 【09-18 修复】支付两个现存生产bug：①配置键名不匹配（代码驼峰 wx_pay_appId 等 vs 库下划线，补3条别名键，此前顾客端真实支付同样受影响）；②WxPayService 请求XML字段 mchId→mch_id（微信V2要求）。修复后微信 native 真实下单、支付宝 precreate 真实下单均通

- 【09-18 注意】物业费收款走平台商户号代收（微信 1440071102 / 支付宝 2021002144664045），线下结算给物业；G28「物业独立收款码/独立商户」仍待用户拍板

- 【09-18 测试账号】演示业主 13851918986 / 123456（ownerId=1，绑定宜必购物业/阳光花园1号楼1单元101）

## 物业缴费闭环（2026-09-18 G79/G80）
- 业主端：bills.html「去缴费」→ POST /api/property/owner/bills/{id}/pay?channel=wechat|alipay（真实下单，PB+毫秒 单号）→ 二维码 → 轮询 GET /bills/{id}/pay-status?paymentNo=；回调按 PB 前缀分流 confirmPaid（幂等）
- 后台对账：admin/bills.html「缴费记录」tab → GET /api/property/admin/payments?companyId=&status=&keyword=（富化 ownerName/roomNo/billPeriod，status 0待付/1已付/2关闭）
- 表：property_payment（payment_no PK、owner_id、bill_id、bill_no、company_id、room_id、amount、channel、status、pay_time）；DDL db/property_payment.sql
- 收款通道：平台商户号代收（微信 wx_pay_mchId=1440071102 / 支付宝 appId=2021002144664045），线下结算给物业；G28 独立商户待拍板

## 车位费在线缴费（2026-09-18 G82）
- 业主端：bills.html 车位费 tab 去缴费 → POST /parking-fees/{id}/pay?channel=（feeType=1 支付单）→ 二维码 → 轮询 /parking-fees/{id}/pay-status；回调按 PB 前缀分流 confirmPaid 按 fee_type 更新 parking_fee 或 bill
- property_payment 表加 fee_type 列（0=物业费 1=车位费）；旧待付单关闭按 fee_type 隔离（findByBillIdAndFeeTypeAndStatus）
- GET /parking-fees 返回富化 List<Map>（含 plateNumber 从 property_vehicle.plate_no）
- 后台缴费记录 tab 增加「类型」列（物业费/车位费），车位费周期取自 property_parking_fee.period

## 访客临停缴费（2026-09-18 G83）
- 页面：visitor-parking.html 临停计算（本地估算：30分钟免费/5元时/30元日封顶）→ 去缴费 → 支付弹窗（微信/支付宝二维码）→ 轮询 /p1/temp-parking/pay-status
- 后端：POST /api/property/owner/p1/temp-parking/pay?plateNo=&entryTime=&channel= → TP+毫秒+4位随机 支付单 → 真实下单；回调按 TP 前缀 → PropertyTempParkingService.onPaymentSuccess（10% 分账）；GET /p1/temp-parking/pay-status?paymentNo=
- 表：property_temp_parking_payment（payment_no/vehicle_log_id(无记录置0)/plate_no/community_id/duration_min/fee_amount/paid_amount/pay_channel/wx_transaction_id/status/split_amount/pay_time）；库名 ybgtc.com
- 自动缴费：auto-pay 绑定/解绑已实现（property_auto_pay_bindding 表），但车辆出场自动扣费调度未做（遗留）
- 生产 bug 修复：微信 V2 回调字段 transaction_id 未落库（代码读 transactionId）→ 加回退读取

## 临停缴费后台对账（2026-09-19 G84）
- admin/temp-parking.html「临时车缴费」：缴费单号/车牌/时长/应收(feeAmount)/实付(paidAmount)/渠道/分账(splitAmount)/时间(payTime)/状态(0待付 1已付)，汇总笔数/总收入/分账总额，小区+日期筛选
- 接口 GET /api/property/admin/p1/temp-payments（communityId|community_id、startDate|start_date、endDate|end_date 全可选，空则全量）；TP 单 community_id 取业主首个绑定公司（bindRelation）
- 部署铁律：pkill 后必须等 8085 端口释放（ss -ltn | grep :8085）再 start，否则新进程 Port in use 退出且健康检查误报

## 业主端写操作链路约定（2026-09-19 G90）
- 业主端页面 req 封装：POST/PUT 统一 application/x-www-form-urlencoded（URLSearchParams）——后端 P1 端点全用 @RequestParam，勿改回 JSON body
- 工单状态机：0待接单→1处理中→2待验收→3已完成(评价后)/4已关闭；评价 PUT /p1/workorders/{id}/rate（rating+comment）仅 status=2 可评
- 工单操作端点：/p1/workorders/{id}/{assign,transfer,complete,close}（后台，handler JSON body 兼容）；列表/详情返回 Map 带 logs（woLog）
- 访客车辆/增值服务参数可选化：visitor-vehicles（plateNo/visitorName 必填，phone/community/room/时间可选，room/community 由 owner 绑定推断）；vs-orders（serviceId 必填，company/room/时间可选推断，默认 appt=now+1h）；value-services 列表带 X-Owner-Token 自动定位公司
- 增值服务状态：0待支付 1已支付 3已完成 4已取消 5已退款

## 工单系统（2026-09-19 G89 修复）
- 类型数字体系：1=报修 2=投诉 3=建议 4=咨询（后端 parseWoType 兼容字符串别名）；业主提交 POST /p1/workorders（woType 参数 String）、后台列表/筛选 /p1/workorders?type=
- 后台列表返回 Map 带 submitter（业主名/家庭成员名）；页面字段用 camelCase（woNo/woId/woType/assigneeName）
- 全页面巡检结论：后台 19 页 + 业主 14 页 JS/fetch 全部干净（G89 起）

## 后台预约退款（2026-09-19 G88）
- 端点：POST /api/property/admin/reservations/{id}/refund（payStatus 1→2 + status→3，幂等拦截）；/reservations/{id}/cancel（已付一并标记退款）
- facilities.html 预约记录操作：确认/签到/完成 + 退款（已付待确认/已确认）+ 取消
- 退款为状态标记（payStatus=2），未接微信/支付宝原路退回（V2 退款需商户证书，未配置）

## 业主预约支付（2026-09-19 G87）
- 业主端点 /api/property/owner/p1：POST /reservations 返回 Map 含 pay（收费时自动下单 RS）；POST /reservations/{id}/pay（换渠道/补单）；GET /reservations/pay-status?paymentNo=
- customer/reservation.html：收费弹支付窗（微信/支付宝切换+轮询）；我的预约待支付显示「去支付」
- 支付单统一 RS 前缀，回调路由（WxPayService/AlipayService）已支持，业主/商家预约共用

## 场地预约双来源（2026-09-19 G86）
- 实体 property_reservation：reserved_type(1业主 2商家)/merchant_no/payment_no；property_company.reservation_fee_enabled(0免费 1收费，后台配置)
- 商家预约：/api/merchant/property/{companies,facilities,facilities/{id}/slots,reservations(POST/GET),reservations/{id}/cancel,reservations/{id}/pay-status}，X-Merchant-Token 鉴权；商家页 /h5/property/merchant/reservation.html（磁盘直放）；商家端入口在 backend static h5/merchant/index.html footer（打包内）
- 收费规则：免费公司 amount=0/payStatus=1 直接登记；收费公司 amount=小时×场地单价/payStatus=0 + RS-支付单（微信 nativePay/支付宝 precreate）→ 回调 RS 前缀 → onPaymentSuccess
- 后台：facilities.html 预约记录 tab（来源列 业主/商家+商家号、收费开关按钮 PUT /companies/{id}/reservation-fee）
- 业主预约保持不变（/api/property/owner/p1/reservations，reservedType=1）；业主端暂不接预约支付

## 分账与抵扣视图（2026-09-19 G85）
- admin/splits.html「分账流水」：/p1/split-records（companyId/splitMode 可选），状态徽章 0待结算/1已结算/3退款冲正/4失败；抵扣日志 /admin/deduct-logs?ownerId=
- 扣款引擎 PropertyDeductEngineService.onSplitSettled：分账结算→业主钱包 balance→按绑定顺序自动扣物业费/车位费（bill.deducted / fee.deducted，扣满置 status=2）；manualDeduct 管理员手动扣
- 业主端 bills.html「抵扣日志」tab：/owner/deduct-logs，显示 createTime/sourceType/deductAmount
- 现状：property_deduct_log 无数据；物业支付（PB/TP）不生成分账记录（G28 拍板前平台代收线下结算）；split_records 仅盲盒 OFF- 订单

## 环境与工具链（2026-09-18 更新，换电脑必读）
- 工作根：D:\workspace-java\ybg-master（Z盘已删）
- 生产：121.229.160.2:5366 root + ybg_key；网站根 /www/wwwroot/ybgtc.com/；jar ibigou.jar；8085
- MySQL：ybgtc.com（反引号）/ ybgtc.com / 密码见服务器 .env
- NAS：192.168.31.228 Alan + id_ed25519；/volume1/docker/github-backup/ybg-master
- GitHub：Alan3911971/ybg master，Git Data API 推送（_push_gh.py），token 在 shared-memory.json
- 工具：Python3+paramiko、JDK17(D:\devtools\jdk)、Maven3.9(m2repo=D:\workspace-java\m2repo)、specify.exe
- 前端部署：property 目录 nginx 磁盘直放，scp 覆盖即生效；其它 h5 打包进 jar
- 后端部署：mvn 打包 → SFTP put jar → setsid start.sh 重启 → curl 127.0.0.1:8085 验证
- 四文档：D:\workspace-java\ybg-master\{规矩文档.txt, 采坑日志.txt, shared-memory.json, 知识图谱.md}

## 物业开奖配置（G91 2026-09-19，物业专用）
- 需求：物业后台缺美团/团购/抖音渠道的开奖配置 → 复刻商家奖品池到物业，独立配置不借商家体系
- 表 property_prize_pool（company_id 维度）：prize_type 0普通1折扣券2立减券3普通余额4团购免单余额；channel 0通用1美团2团购3抖音；pool_type 0本店1公共；weight 权重；enabled
- 后端 PropertyAdminController（X-Property-Token）：GET/POST /api/property/admin/prize-pools（companyId 必填，prizeId 可选=编辑）、/prize-pools/{id}/enable|disable、DELETE /prize-pools/{id}、GET /prize-stats?companyId=（按渠道分组 count/enabledCount/totalWeight，channelName 中文）
- 页面 h5/property/admin/prizes.html：公司下拉+渠道 tab 筛选+添加弹层（池类别/类型/面值/权重/渠道）+启停删除+顶部渠道报表卡片；侧边栏🎁开奖配置 + 首页快捷入口
- 渠道枚举：0通用 1美团 2团购 3抖音（校验 0-3）；prizeType 校验 0-4
- 与商家奖品池（BoxPrizePool/merchantNo 维度）完全隔离，触发抽奖逻辑待用户后续拍板（当前交付配置管理+统计）
